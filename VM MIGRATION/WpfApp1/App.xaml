<Application x:Class="WpfApp1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp1"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- ========================================================= -->
        <!-- 1. COLOR PALETTE                                          -->
        <!-- ========================================================= -->
        <Color x:Key="ColorBackgroundDark">#060B12</Color>
        <Color x:Key="ColorSidebarDark">#04080D</Color>
        <Color x:Key="ColorCardDark">#0D1929</Color>
        <Color x:Key="ColorCardBorder">#192E47</Color>
        <Color x:Key="ColorInputDark">#091320</Color>
        <Color x:Key="ColorInputBorder">#162B44</Color>
        <Color x:Key="ColorCyanAccent">#0098B9</Color>
        <Color x:Key="ColorCyanAccentHover">#00B6DE</Color>
        <Color x:Key="ColorCyanAccentPressed">#007892</Color>
        <Color x:Key="ColorTextWhite">#FFFFFF</Color>
        <Color x:Key="ColorTextSecondary">#8DA4BE</Color>
        <Color x:Key="ColorTextMuted">#4D6683</Color>
        <Color x:Key="ColorSuccess">#10B981</Color>
        <Color x:Key="ColorWarning">#F59E0B</Color>
        <Color x:Key="ColorDanger">#EF4444</Color>

        <!-- SOLID BRUSHES -->
        <SolidColorBrush x:Key="BrushBackgroundDark" Color="{StaticResource ColorBackgroundDark}"/>
        <SolidColorBrush x:Key="BrushSidebarDark" Color="{StaticResource ColorSidebarDark}"/>
        <SolidColorBrush x:Key="BrushCardDark" Color="{StaticResource ColorCardDark}"/>
        <SolidColorBrush x:Key="BrushCardBorder" Color="{StaticResource ColorCardBorder}"/>
        <SolidColorBrush x:Key="BrushInputDark" Color="{StaticResource ColorInputDark}"/>
        <SolidColorBrush x:Key="BrushInputBorder" Color="{StaticResource ColorInputBorder}"/>
        <SolidColorBrush x:Key="BrushCyanAccent" Color="{StaticResource ColorCyanAccent}"/>
        <SolidColorBrush x:Key="BrushCyanAccentHover" Color="{StaticResource ColorCyanAccentHover}"/>
        <SolidColorBrush x:Key="BrushCyanAccentPressed" Color="{StaticResource ColorCyanAccentPressed}"/>
        <SolidColorBrush x:Key="BrushTextWhite" Color="{StaticResource ColorTextWhite}"/>
        <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource ColorTextSecondary}"/>
        <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}"/>
        <SolidColorBrush x:Key="BrushSuccess" Color="{StaticResource ColorSuccess}"/>
        <SolidColorBrush x:Key="BrushWarning" Color="{StaticResource ColorWarning}"/>
        <SolidColorBrush x:Key="BrushDanger" Color="{StaticResource ColorDanger}"/>

        <!-- ========================================================= -->
        <!-- 2. CYBER-GLASS GRADIENTS & NEON GLOWS                     -->
        <!-- ========================================================= -->
        <LinearGradientBrush x:Key="BrushCardGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0F2036" Offset="0.0"/>
            <GradientStop Color="#0A1524" Offset="1.0"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrushCardBorderGlow" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#25466B" Offset="0.0"/>
            <GradientStop Color="#122438" Offset="0.5"/>
            <GradientStop Color="#1D3958" Offset="1.0"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrushCyanBtnGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00B6DE" Offset="0.0"/>
            <GradientStop Color="#0083A3" Offset="1.0"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrushCyanBtnHoverGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#38D0F5" Offset="0.0"/>
            <GradientStop Color="#00A2C7" Offset="1.0"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BrushGaugeGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#00B6DE" Offset="0.0"/>
            <GradientStop Color="#10B981" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- NEON GLOW DROP SHADOW EFFECTS -->
        <DropShadowEffect x:Key="EffectCyanGlow" BlurRadius="20" ShadowDepth="0" Color="#00B6DE" Opacity="0.30"/>
        <DropShadowEffect x:Key="EffectGreenGlow" BlurRadius="18" ShadowDepth="0" Color="#10B981" Opacity="0.32"/>
        <DropShadowEffect x:Key="EffectButtonGlow" BlurRadius="14" ShadowDepth="0" Color="#00B6DE" Opacity="0.45"/>
        <DropShadowEffect x:Key="EffectModalShadow" BlurRadius="30" ShadowDepth="8" Color="#000000" Opacity="0.80"/>

        <!-- ========================================================= -->
        <!-- 3. VECTOR GEOMETRIES                                      -->
        <!-- ========================================================= -->
        <Geometry x:Key="GeoOrchestrator">M3,3H10V10H3V3M12,3H19V10H12V3M3,12H10V19H3V12M12,12H19V19H12V12Z</Geometry>
        <Geometry x:Key="GeoInventory">M4,4H20A2,2 0 0,1 22,6V8A2,2 0 0,1 20,10H4A2,2 0 0,1 2,8V6A2,2 0 0,1 4,4M4,12H20A2,2 0 0,1 22,14V16A2,2 0 0,1 20,18H4A2,2 0 0,1 2,16V14A2,2 0 0,1 4,12M6,6V8H8V6H6M6,14V16H8V14H6Z</Geometry>
        <Geometry x:Key="GeoPulse">M3,12H6.5L9,5L13,19L16,10L18,12H22</Geometry>
        <Geometry x:Key="GeoSettings">M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.67 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z</Geometry>
        <Geometry x:Key="GeoServerStack">M4,1H20A1,1 0 0,1 21,2V6A1,1 0 0,1 20,7H4A1,1 0 0,1 3,6V2A1,1 0 0,1 4,1M4,9H20A1,1 0 0,1 21,10V14A1,1 0 0,1 20,15H4A1,1 0 0,1 3,14V10A1,1 0 0,1 4,9M4,17H20A1,1 0 0,1 21,18V22A1,1 0 0,1 20,23H4A1,1 0 0,1 3,22V18A1,1 0 0,1 4,17M6,3V5H8V3H6M6,11V13H8V11H6M6,19V21H8V19H6Z</Geometry>
        <Geometry x:Key="GeoTerminal">M4,4H20A2,2 0 0,1 22,6V18A2,2 0 0,1 20,20H4A2,2 0 0,1 2,18V6A2,2 0 0,1 4,4M6,8L10,12L6,16H8.5L12.5,12L8.5,8H6M13,15V17H18V15H13Z</Geometry>
        <Geometry x:Key="GeoLightning">M11,21L12.5,13H7L14,3L12.5,11H18L11,21Z</Geometry>
        <Geometry x:Key="GeoServerContainer">M4,2H20A2,2 0 0,1 22,4V9A2,2 0 0,1 20,11H4A2,2 0 0,1 2,9V4A2,2 0 0,1 4,2M4,13H20A2,2 0 0,1 22,15V20A2,2 0 0,1 20,22H4A2,2 0 0,1 2,20V15A2,2 0 0,1 4,13M6,5V7H8V5H6M6,16V18H8V16H6Z</Geometry>

        <!-- ========================================================= -->
        <!-- 4. TOOLTIPS & SCROLLBARS                                  -->
        <!-- ========================================================= -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#091422"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushCyanAccentHover}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Effect" Value="{StaticResource EffectCyanGlow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#172A42"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="{x:Type Thumb}">
                                                <Border Background="#1C3552" CornerRadius="4">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{StaticResource BrushCyanAccentHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================================= -->
        <!-- 5. GRADIENT TELEMETRY GAUGES (PROGRESS BARS)              -->
        <!-- ========================================================= -->
        <Style TargetType="{x:Type ProgressBar}">
            <Setter Property="Background" Value="#0A1420"/>
            <Setter Property="Foreground" Value="{StaticResource BrushGaugeGradient}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" ClipToBounds="True">
                            <Grid x:Name="TemplateRoot">
                                <Border x:Name="PART_Track" Background="Transparent"/>
                                <Border x:Name="PART_Indicator" HorizontalAlignment="Left" Background="{TemplateBinding Foreground}" CornerRadius="4"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================================= -->
        <!-- 6. LISTBOX & LISTVIEW STYLING                             -->
        <!-- ========================================================= -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="b" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True" CornerRadius="6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#0F243B"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#0F3152"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#091420"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushCardBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#0F1E30"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <GridViewRowPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#10253D"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0D3054"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================================= -->
        <!-- 7. INPUT TEXTBOXES & PASSWORDBOXES                        -->
        <!-- ========================================================= -->
        <Style x:Key="EnterpriseInputTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BrushInputDark}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushInputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="{StaticResource BrushCyanAccentHover}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <Grid>
                                <TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" Foreground="{StaticResource BrushTextMuted}" Margin="12,8" IsHitTestVisible="False" VerticalAlignment="Center" Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False" Margin="2,0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushCyanAccent}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushCyanAccentHover}"/>
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource EffectCyanGlow}"/>
                                <Setter TargetName="placeholder" Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EnterpriseInputPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource BrushInputDark}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushInputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="{StaticResource BrushCyanAccentHover}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <Grid>
                                <TextBlock x:Name="placeholder" Text="{TemplateBinding Tag}" Foreground="{StaticResource BrushTextMuted}" Margin="12,8" IsHitTestVisible="False" VerticalAlignment="Center" Visibility="Visible"/>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False" Margin="2,0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushCyanAccent}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushCyanAccentHover}"/>
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource EffectCyanGlow}"/>
                                <Setter TargetName="placeholder" Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================================= -->
        <!-- 8. ACTION & OUTLINE BUTTONS                               -->
        <!-- ========================================================= -->
        <Style x:Key="CyanActionBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrushCyanBtnGradient}"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource BrushCyanBtnHoverGradient}"/>
                                <Setter TargetName="btnBorder" Property="Effect" Value="{StaticResource EffectButtonGlow}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="{StaticResource BrushCyanAccentPressed}"/>
                                <Setter TargetName="btnBorder" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="btnBorder" Property="Background" Value="#17283C"/>
                                <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}"/>
                                <Setter TargetName="btnBorder" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SlateOutlineBtn" TargetType="Button">
            <Setter Property="Background" Value="#142438"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BrushCardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="{StaticResource BrushCyanAccentHover}"/>
                                <Setter TargetName="btnBorder" Property="Background" Value="#1B3454"/>
                                <Setter TargetName="btnBorder" Property="Effect" Value="{StaticResource EffectCyanGlow}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="btnBorder" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================================= -->
        <!-- 9. SIDEBAR NAVIGATION                                     -->
        <!-- ========================================================= -->
        <Style x:Key="SidebarNavRadioBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="pillBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}" BorderThickness="1" BorderBrush="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="pillBorder" Property="Background" Value="#0E1E30"/>
                                <Setter Property="Foreground" Value="{StaticResource BrushTextWhite}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="pillBorder" Property="Background" Value="#0F2C4A"/>
                                <Setter TargetName="pillBorder" Property="BorderBrush" Value="{StaticResource BrushCyanAccentHover}"/>
                                <Setter TargetName="pillBorder" Property="Effect" Value="{StaticResource EffectCyanGlow}"/>
                                <Setter Property="Foreground" Value="{StaticResource BrushCyanAccentHover}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>